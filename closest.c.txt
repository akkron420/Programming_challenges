#include <stdlib.h>

/*
  Write a function for printing 5 closest numbers
*/
void printclosest(int arr[], int num, int n){
  int temp;
  int min;
  int j;
  for (j = 0; j < sizeof(arr)+2; j++ ) {
      printf("el %d", arr[j]);
  }

   int minarr[MAXSIZE] = {0};
   int first = 0;
   int last = n - 1;
   int middle = (first+last)/2;
  
     if (arr[middle] < num) { 
       
       for (middle;middle <n ;middle++){
         temp = abs(arr[middle]-num);
         minarr[middle] = temp;
         printf("   ---- %d ------ diff of %d and %d",temp,arr[middle],num);
         //printf("%d is num and minsub is %d at index %d",arr[middle],minarr[middle],middle);
       }
     }  
     else{
       for (first;first<middle;first++){
         temp = abs(arr[first]-num);
         minarr[first] = temp;
         printf("   ---- %d ------ diff of %d and %d",temp,arr[first],num);
         
       }
     }
    
    int l;
    for (l=0;l < 20;l++) {
    printf("%d\n",minarr[l]);
   }
    int  k = 0;
    int m;
    min = minarr[k];

    for (m = 0; m < n; ++m)
    {
        if (minarr[m] < min)
        {
            min = (int)minarr[m];
            k = m;
        }
    }
    printf("min index at %d",k);
   
}
